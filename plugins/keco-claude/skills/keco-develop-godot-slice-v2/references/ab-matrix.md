# Historical comparison note

This reference is retained only as a record of the former comparison exercise.
It is not an active routing or runtime contract. The current workflow provides implicit, document-driven routing. New work uses
`keco-develop-godot-slice-v2` and `KECO_OBSERVATION` exclusively.
